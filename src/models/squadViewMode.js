/**
 * Shared value for Bet / Ranks / Audit dropdowns (IPL match betting vs adhoc bets).
 * Use the same state everywhere so the mode stays consistent across tabs.
 */
export const SQUAD_VIEW_BET = 'bet';
export const SQUAD_VIEW_ADHOC_BET = 'adhoc_bet';
